version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci # Use lockfile for reproducible CI builds
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
